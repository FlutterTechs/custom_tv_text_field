name: example
description: Example application for custom_tv_text_field.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.10.3 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  custom_tv_text_field:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
